Backend way of walking with typescript

npm init -y
npm i express
npm i -D typescript // avoid installing typescript globally because of versioning so you don't encounter issues in the future
npm i -D @types/express
npx tsc --init   // Configuring the compiler
npx ts --build
node ./dist/index.js
npm i -D nodemon 
npm i _d ts-node

npm i express-session

npm i -D jest
npm i -D ts-jest @types/jest
npx ts-jest config:init

src - index.ts
import express from 'express'
import express from "express-serve-static-core"  // use this because of the new version of typescript
we don't want to use commonjs with typescript
one of the way is by transpiling typescript to javascript
npx tsc --init // using the local typescript that came with the dependency. It will generate the ts config file
In the config file, look for rootDir and uncomment
Also for outDir - where your javascript code will be generated to
noimplicitAny: true
strictNullChecks: true
function: true


dto: data transfer object

